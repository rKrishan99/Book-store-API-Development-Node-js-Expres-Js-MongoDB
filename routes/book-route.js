const express = require('express');

//create express router
const router = express.Router();

//all routers that are related to books only
router.get('/get',);
router.get('/get/:id',);
router.post('/add',);
router.put('/upadate/:id',);
router.delete('/delete/:id',);